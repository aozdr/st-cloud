# TASK：SV-001 schema_version 表 + 迁移执行

> 优先级 P1。关联 State: `.ai/state/20260812-schema-versioning.yaml`

## 目标
新建 `schema_version` 表记录每次迭代的版本号与执行的 SQL 文件清单，并执行到 MySQL。

## 修改范围
- `docker/mysql/init/31_schema_version.sql`（已存在）：CREATE TABLE + 基线 INSERT + 当前迭代 INSERT
- `st-core/src/test/resources/schema.sql`：补 `schema_version` 表定义（H2 兼容）
- 执行 31 号脚本到运行中 MySQL

## 禁止修改范围
- 不改现有业务表结构
- 不改现有迁移脚本内容

## 验收标准
- MySQL `schema_version` 表存在且有 2 条基线记录
- H2 schema.sql 含 schema_version 表，集成测试启动不报错
- SchemaConsistencyTest 全绿

## 测试要求
- 全量回归 127+ 用例通过

## 输出要求
- 完成后产出 `.ai/docs/20260812-schema-versioning/changereport-sv001.md`