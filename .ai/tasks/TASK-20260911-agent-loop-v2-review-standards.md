# TASK：Agent Loop V2 Standards Review

## 目标

以 `HEAD=b97faa5854596b5e9d9a4af64f30517d209db501` 为固定点，只读审查当前 Agent Loop V2 相关工作区改动是否符合仓库规范与通用代码气味基线。

## 范围

- 设计与 TASK 指定的 Agent Loop V2 文件
- 同一工作区内作为 V2 前置基线的 `20260910-ai-process-optimization` 改动

## 禁止

- 不修改任何文件、State、Git 配置或 Git 历史
- 不审查业务代码

## 输出

- 按严重度列出 Standards 发现；区分硬违规与判断项；提供文件和行号。
