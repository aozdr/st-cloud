# TASK：SV-003 AGENTS.md DB 版本规则

> 优先级 P1。关联 State: `.ai/state/20260812-schema-versioning.yaml`

## 目标
在 AGENTS.md 中新增数据库版本管理规则，强制每次迭代：更新版本号、H2 通过后对比正式 DB、记录需执行的 SQL 文件。

## 修改范围
- `AGENTS.md`：新增「数据库版本管理」章节

## 禁止修改范围
- 不改现有 7 条代码修改约束与 Loop 流程定义

## 验收标准
- AGENTS.md 含明确规则：每次迭代更新 schema_version 版本号 + H2 测试通过后运行 compare-schema.ps1 + 记录 SQL 文件清单
- verify-loop PASS

## 输出要求
- 完成后产出 `.ai/docs/20260812-schema-versioning/changereport-sv003.md`