# TASK：SV-002 H2/MySQL schema 对比脚本

> 优先级 P1。关联 State: `.ai/state/20260812-schema-versioning.yaml`

## 目标
新建脚本对比 H2 schema.sql 与 MySQL 实际 schema 的列差异，并输出当前迭代需执行的 SQL 文件清单。

## 修改范围
- `.ai/scripts/compare-schema.ps1`（新增）：对比 H2 schema.sql 列集与 MySQL INFORMATION_SCHEMA 列集，输出差异与待执行 SQL 文件清单

## 禁止修改范围
- 不改 SchemaConsistencyTest 既有逻辑

## 验收标准
- 脚本可独立运行，输出：H2 有/MySQL 缺的列、MySQL 有/H2 缺的列、待执行 SQL 文件清单
- 无差异时输出 PASS

## 测试要求
- 手动运行验证输出正确

## 输出要求
- 完成后产出 `.ai/docs/20260812-schema-versioning/changereport-sv002.md`