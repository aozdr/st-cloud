# 浏览器体验验收记录

- 日期：2026-09-23
- 执行者：GPT-6 主线程（自检，不是独立体验评审）
- 浏览器：Codex 内嵌浏览器
- 当前任务：TASK-20260923-team-search-watch

## 检查结果

| 场景 | 结果 | 证据 |
|---|---|---|
| 团队搜索移动端筛选 | 通过 | 视口 375×812；文档 `scrollWidth=375`；筛选项保持横向布局、文字未竖排；Space 键可将“压缩包”筛选切换为选中 |
| 通知铃铛移动端可达 | 通过 | 375px 视口下全局顶栏显示通知按钮 |
| 通知弹层边界与键盘关闭 | 通过 | 弹层测得 left=12px、right=363.2px，均在视口内；Escape 关闭后焦点回到触发按钮 |
| 通知列表空状态 | 通过 | 空数据时显示“暂无通知” |

## 环境边界

本次验证覆盖浏览器布局、键盘与空状态交互。测试站点使用当时可用的登录状态/前端响应，不能据此声称真实文件服务、MySQL、Elasticsearch 与对象存储组成的端到端链路已通过；后端权限、投递和 Outbox 由 H2/单元集成测试覆盖。未保存截图文件，因此本记录只引用实际测得的 DOM/视口值。

## r8 主线程复查（2026-09-23）

前端开发服务器首次在受限沙箱内因 esbuild `spawn EPERM` 启动失败；经工具权限批准后在 `127.0.0.1:5173` 启动，HTTP 返回 200，Codex 内嵌浏览器能够打开。这解决了此前“浏览器无法打开”的环境问题。浏览器可见的首页和 `/search` 可进入；切换“团队文件”后显示选择团队空间控件，但后端 `127.0.0.1:8080` 未运行，界面提示 `Network Error`，无法进行真实搜索结果验收。通知弹层显示“通知加载失败”和重试按钮；按 Escape 可关闭并把焦点还给通知按钮。

该次仅连接前端的复查只证明页面可打开及错误态/键盘行为；后续补齐的可重复浏览器验收见下节。

## r8 可重复浏览器验收（用户授权单人执行）

在本机 Vite 页面运行仓库现有 `st-web/scripts/team-watch-ui-smoke.py`，通过 Playwright 拦截 `/api/**` 为测试 fixture，实际驱动渲染后的 React 路由。脚本覆盖 375px 团队搜索旧响应不能覆盖新查询、游标过期且保留已有结果时出现“重新搜索”并从第一页请求、失效关注记录脱敏与空 DELETE、个人文件定位、旧 FILE_CHANGE 通知不误走新 target 接口，以及通知 A/B 目标响应乱序时仅导航 B。退出码 0，输出 `team-watch UI smoke: PASS`。

这项验收与后端单元/H2/真实临时 ES 测试组合覆盖前后端契约，仍不代表真实登录会话连通 MySQL、ES、MQ、S3 的部署级端到端测试。根据当前 uiSpec 的页面与状态范围，主线程体验自检通过；该结论按用户授权登记，不能称为独立体验评审。
