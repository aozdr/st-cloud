# 续做核查记录

日期：2026-09-19。主线程只做文档、核查与串行验证，编码及测试代码由 GPT-5.6-Luna max 完成。

- 已按当前 Dispatch/State 协议重新建立本次 TASK、State 与设计产物，保留已有代码；历史完成声明不作为本次验收证据。
- 本地 Elasticsearch 8.12.0 可访问。MySQL schema 基线对比退出码 0，日志 schema-baseline.log；新增迁移尚未执行。
- 发现并交回 Luna 的待核查问题：删除权限链断裂/深度耗尽、失效通知 refId、现有部分后端编译缺口、并发关注的 MySQL 当前读、索引元数据更新不能给旧正文覆盖新 MD5、返回前内容版本与目录范围复核。
- 前端待核查：切换节点时 saving 状态、取消订阅后分页偏移、严格 available 判定、团队目标 StrictMode 竞态、个人定位参数消费。
- 当前尚未完成：集成构建、数据库迁移双对比、真实团队搜索测试、浏览器体验验收、独立评审与最终验收。
