# 测试报告：历史版本预览

> Task: `20260910-version-preview`｜tester｜日期：2026-09-10
> 环境：Windows / PowerShell 7.6.4 / JDK 17.0.14 (Liberica) / Maven 3.9.11 / Node 24.16.0 / npm 11.13.0

## 一、执行结果

| 命令 | 范围 | 结果 |
|------|------|------|
| `mvn -pl st-preview -am test` | st-common 31、st-core 149、st-preview 14 | 通过，0 失败 0 错误 |
| `npm run build`（st-web） | tsc -b + vite build | 通过 |
| `mvn -q -DskipTests compile` | 全仓模块编译 | 通过（退出码 0） |

合计 194 个用例通过。

## 二、用例覆盖（对照 testcases.md）

| 编号 | 场景 | 结果 |
|------|------|------|
| TC-01 | 文本历史版本返回该版本内容 | 通过 |
| TC-02 | 视频历史版本返回版本预签名 URL | 通过 |
| TC-03 | 图片历史版本缩略图 key 含版本号 | 通过（断言 `thumbnails/{nodeId}/v2/lg.jpg`） |
| TC-04 | 版本不属于该文件 → 版本不存在 | 通过 |
| TC-05 | versionId 不存在 → 版本不存在 | 通过 |
| TC-06 | 节点无权限 → 无权限异常 | 通过 |
| TC-07 | Office 历史版本 → unsupported | 通过 |
| TC-08 | 当前版本预览回归 | 通过 |
| TC-09 | 文件夹节点拒绝预览 | 通过 |
| FE-01~FE-04 | 前端入口/版本模式行为 | 代码级确认 + 构建通过；浏览器点击验证未执行 |
| FE-05 | 前端构建 | 通过 |
| M-01~M-04 | 手工验收（不改当前版本、不写最近文件、层级、旧接口回归） | 未执行，需运行完整环境 |

## 三、失败与修复记录

| 轮次 | 现象 | 原因 | 处理 |
|------|------|------|------|
| 第 1 轮 | 5 个用例失败/报错 | 共享 Mock 桩泄漏：无权限用例 stub 的异常残留；上个测试类的 `headObject` 404 桩影响图片用例 | 用例内统一 `@AfterEach reset(...)`，图片用例显式桩定 `headObject` |
| 第 2 轮 | 全部通过 | — | — |

## 四、未覆盖与说明

- 浏览器端手工验收（M-01 ~ M-04）需要 MySQL/Redis/S3/后端/前端全部运行，本次未启动，未做点击验证；建议在验收环节人工确认。
- 未做性能压测；版本预览与当前版本预览同路径，单请求 2 次主键查询，无新增复杂度。
