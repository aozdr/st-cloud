# ADR-xxx：<决策标题>

> 架构决策记录（Architecture Decision Record）。重要架构 / 数据模型 / 接口契约决策须记录，存放于 `.ai/decisions/ADR/`，编号递增。由 Knowledge Manager 复核一致性。

## 状态

Accepted / Proposed / Deprecated

## 背景

（为什么需要做这个决策：问题、约束、触发场景）

## 决策

（最终采用方案，具体到可执行）

## 放弃的方案

（列出曾考虑但未采用的方案）

## 理由

（为何选择此方案：性能 / 安全 / 扩展性 / 实现成本等权衡）

## 后续限制

（本决策带来的约束、依赖、需要后续关注的点）

## 关联

- 关联任务 State: `.ai/state/<task-id>.yaml`
- 关联文档: `.ai/docs/<task-id>/design.md`